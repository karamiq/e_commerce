$kind: http-request
url: "{{baseUrl}}/roles/{{roleId}}"
method: PATCH
headers:
  Content-Type: application/json
body:
  type: text
  content: |-
    {
      "name": "manager",
      "description": "Updated description"
    }
order: 4000
