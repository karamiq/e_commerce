$kind: http-request
url: "{{baseUrl}}/employees/{{employeeId}}/role"
method: PATCH
headers:
  Content-Type: application/json
body:
  type: text
  content: |-
    {
      "roleId": "{{roleId}}"
    }
order: 5000
